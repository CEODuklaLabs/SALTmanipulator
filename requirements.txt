# SALT Manipulator – Python dependencies
#
#       pip install -r requirements.txt
#
# Na Raspberry Pi musí být povoleno I2C:
#       sudo raspi-config  -> Interface Options -> I2C -> Enable
# (nebo: bash setup.sh --hardware)
#
# Názvy knihoven desek se konfigurují v config.INPUT_BOARDS / config.OUTPUT_BOARD.

# ── Web dashboard ─────────────────────────────────────────────────────────────
flask>=3.0

# ── I/O desky Sequent Microsystems (Raspberry Pi) ───────────────────────────
# hardware.py na ně mluví přímo přes smbus (adresy/registry v config.py),
# knihovny SM* nejsou potřeba. smbus bývá součástí python3-smbus (apt).

# ── UPS (Suptronics X1205) ────────────────────────────────────────────────────
smbus2>=0.4.0       # I2C komunikace s MAX17048 fuel gauge
gpiod>=1.5.0        # GPIO čtení stavu AC napájení (pin 6)

# ── Arduino serial driver (ADRU/code.cpp) ─────────────────────────────────────
pyserial>=3.5       # Sériová komunikace s Arduino UNO/Mega